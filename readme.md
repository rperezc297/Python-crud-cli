# crudpy

Its a simple CRUD in python CLI with mysql.

### Requeriments (software)
sudo apt install python3-pip

pip3 install mysql-connector-python

### Import database
Import database python.sql and change user and password on Database.py